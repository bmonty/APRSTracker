This is initial work to create a native MacOS application that can display APRS data.
Data is acquired over a network connection to a TNC that outputs frames using the
KISS protocol.  All code is written in Swift 4.2.
